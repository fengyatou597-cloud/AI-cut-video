# 普通用户使用说明

这份说明给不懂代码的人使用。

## 第一次使用

1. 双击 `check_environment.bat`。
2. 如果提示缺少 Python，请安装 Python 3.11 或更高版本，并勾选 `Add python.exe to PATH`。
3. 双击 `install_windows.bat`。
4. 等它显示安装完成。

如果这个包里已经带了 `frontend/dist`，普通使用不需要安装 Node.js。只有网页构建文件缺失、或者你要改前端代码时，才需要安装 Node.js 20 或更高版本。

## 每次启动

双击：

```text
start_windows.bat
```

它会打开一个黑色窗口，负责同时提供后端服务和网页页面。

浏览器会自动打开：

```text
http://localhost:8000
```

使用结束后，关闭这个黑色窗口即可。

## 最快试用

打开首页后，点击：

```text
生成示例项目
```

系统会自动创建一个示例项目，并生成：

- 文稿
- 分镜表
- timeline

然后你可以从左上角或项目页进入：

1. 分镜表：看系统如何拆段、拆镜头。
2. 素材库：上传你自己的视频、图片、音频。
3. Timeline：重新生成 timeline，看素材如何进入轨道。
4. 剪映草稿：检查环境并导出。

## 正式使用流程

1. 首页点击 `创建新项目`。
2. 填视频标题、平台、风格、节奏。
3. 上传文稿，支持 `.txt` 和 `.md`。
4. 上传你提前准备好的 `.srt` 字幕。没有 SRT 也能跑，但字幕时间会比较粗。
5. 点击生成 storyboard。
6. 到分镜表页面检查和修改。
7. 到素材库上传视频、图片、旁白录音、音乐。第一个音频会作为主旁白轨。
8. 到 Timeline 页面生成粗剪 timeline。系统会优先按 SRT 字幕时间、旁白音频和素材库生成轨道。
9. 到剪映草稿页面生成草稿。页面显示“剪映草稿”时，直接打开剪映，在本地草稿列表找同名草稿继续剪。

## 剪映草稿路径怎么设置

点击左侧：

```text
本地设置
```

点击“扫描候选路径”。如果找到推荐路径，点击“使用这个路径”。也可以手动填写剪映草稿目录。通常目录名类似：

```text
com.lveditor.draft
```

如果扫描不到，可以先打开剪映，创建一个空草稿，然后回到本地设置重新扫描。当前项目已经把 pyJianYingDraft 写入依赖；只要你填对剪映草稿目录，系统会尝试直接生成剪映草稿。如果路径没填或不可用，系统仍然能输出 fallback 包，包括：

- storyboard.json
- timeline.json
- subtitles.srt
- draft_generation_notes.md

注意：成功生成剪映草稿后，不需要把 ZIP 导入剪映。ZIP 只是备份。真正的剪映草稿已经写进 `com.lveditor.draft` 目录，剪映会从自己的草稿列表里读取它。

## AI API 怎么设置

不要把 key 发到聊天里。进入左侧：

```text
本地设置
```

在 `AI API 设置` 里填写：

- 通用 API Base URL / API Key
- 文本模型 Base URL / API Key / 模型名
- 视觉模型 Base URL / API Key / 模型名

如果文本和视觉来自同一个平台，只填通用配置和两个模型名即可。如果来自不同平台，就分别填文本和视觉那两组。

保存后：

- 生成 storyboard 会优先用真实 AI 理解文稿。
- 素材库可以点击 `AI 识别素材`，让 AI 看缩略图/关键帧并生成标签。
- 如果 API 调用失败，系统会回退到本地 mock，不影响继续出剪映草稿。

## 什么时候需要安装 ffmpeg

如果你想让系统自动识别视频时长、分辨率、关键帧缩略图，需要安装 ffmpeg。

如果没装，素材仍然能上传，只是视频分析信息会少一些。

## 目前还不是全自动成片

当前版本做的是“粗剪助手”：

- 帮你拆文稿。
- 帮你生成分镜。
- 帮你整理素材、SRT 字幕、旁白录音和 timeline。
- 给镜头分配基础推拉、滑动、叠化、闪切、文字入场等粗剪动效策略。
- 尝试生成剪映草稿。

它不会：

- 自动点击剪映。
- 自动导出最终视频。
- 自动帮你找素材。
- 自动替你确认版权。

最终仍建议在剪映里人工微调和导出。

## 出问题怎么办

优先看页面上的提示。尤其是“剪映草稿”页面，它会告诉你：

- 有没有 storyboard。
- 有没有 timeline。
- 剪映草稿目录有没有配置。
- pyJianYingDraft 是否可用。
- ffmpeg / ffprobe 是否可用。
- 当前会直接生成剪映草稿，还是输出 fallback 包。

如果安装失败，把黑色窗口里的错误文字截图或复制出来即可。



## 页面提示 Failed to fetch 怎么办

这通常不是你点错了，而是前端页面连不上后端服务。

按这个顺序处理：

1. 关闭已经打开的本地服务黑色窗口。
2. 重新双击 `start_windows.bat`。
3. 等它提示 local website is ready，或浏览器自动打开。
4. 浏览器打开后刷新页面。

如果还是失败：

1. 打开浏览器访问 `http://localhost:8000/api/health`。
2. 如果看不到 `status: ok`，说明后端没启动成功。
3. 打开浏览器访问 `http://localhost:8000`。
4. 如果打不开，查看黑色窗口里的错误提示。

常见原因：

- 没有先运行 `install_windows.bat`。
- 本地服务黑色窗口被关掉了。
- 8000 端口被其他程序占用。
- Python 没有安装好，或者网页构建文件缺失时 Node.js 没有安装好。

## 我想把文件保存到 D 盘

进入左侧 `本地设置`。

有两个路径，不要混在一起：

1. `粗剪包输出目录`

这个可以填 D 盘，例如：

```text
D:\AI_RoughCut_Output
```

fallback 粗剪包会保存到这里，包括：

- storyboard.json
- timeline.json
- subtitles.srt
- draft_generation_notes.md

2. `剪映草稿目录`

这个必须是剪映自己的草稿目录。它可能在 C 盘，也可能在 D 盘，取决于你的剪映安装和草稿保存方式。只有填对这个目录，剪映才可能直接识别生成的草稿。

如果你只是想把本工具输出的文件放到 D 盘，请改 `粗剪包输出目录`。
如果你想让剪映直接看到草稿，请配置 `剪映草稿目录`。

